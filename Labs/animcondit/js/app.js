// Haven Hamelin
// 01/25/23
// N220

//Define posX and posY, along with speedX and speedY, and boundsX and boundsY


function setup() {
    //createCanvas(boundsX, boundsY);
}

function draw() {
    //background(255);
    //Create a circle of arbitrary size with position of (posX,posY)

    //Check if posX has not reached 0 or boundsX, if so add speedX to posX
    
    //Otherwise, multiply speedX by -1

    //Check if posY has not reached 0 or boundsY, if so add speedY to posY

    //Otherwise, multiply speedY by -1


}